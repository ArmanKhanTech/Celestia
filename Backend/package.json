{
  "name": "backend",
  "version": "1.0.0",
  "description": "Blockchain based chat app.",
  "main": "index.js",
  "scripts": {
    "run": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
